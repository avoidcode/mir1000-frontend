import "../styles/Landing.css";
import { Hero, Stats } from "../components";

const Landing = () => {
    return (
        <main>
            <Hero />
            <Stats />
        </main>
    );
};

export default Landing;
